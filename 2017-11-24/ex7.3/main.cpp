#include <iostream>
using namespace std;
// exempel 7.3, s. 84
int main()
{/*
  double hojd;
  cout << "Ange pudelns mankhöjd: ";
  cin >> hojd;
  if ( (hojd > 28) && (hojd < 35) )
    cout << "Det är en dvärgpudel." << endl;
  else
    cout << "Det är INGEN en dvärgpudel." << endl;
  
   if ( (hojd > 28) || (hojd < 35) )
    cout << "Det är INGEN dvärgpudel." << endl;
  else
    cout << "Det är en dvärgpudel." << endl;
 */
 
 if ( !(1>2) )
 {
  cout << "SANT\n"; 
 } 

  return 0;
}
